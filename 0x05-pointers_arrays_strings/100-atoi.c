#include "main.h"
/**
 * _atoi - this function converts strings into integers
 *
 * @s: string to be converted
 * Return: int
 */
int _atoi(char *s)
{
	char w[5];
	int size;

	w = "Kuzon";
	size = _strlen(w);
	return (size);
}

