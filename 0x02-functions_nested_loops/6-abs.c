#include "main.h"
/**
 * _abs - This function computes the absolute value of an integer
 *
 * @i: is the integer to be computed
 *
 * Return: absolute value of n
 */
int _abs(int i)
{
	return ((i < 0) ? -i : i);
}
